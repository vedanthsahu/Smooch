services:
  postgres:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-incidentcommand}
      POSTGRES_USER: ${POSTGRES_USER:-incidentcommand}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-incidentcommand}
    ports:
      # Host port 5433, not 5432 -- this machine already has a native Postgres
      # bound to 5432 (unrelated to this project), so 5432 was silently
      # intercepting connections meant for this container. See git history /
      # commit message for how this was diagnosed.
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-incidentcommand}"]
      interval: 5s
      timeout: 5s
      retries: 10

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-incidentcommand}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-incidentcommand123}
    ports:
      - "9000:9000"   # S3 API
      - "9001:9001"   # console
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
  minio_data:
