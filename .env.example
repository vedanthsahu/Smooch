# Copy to .env (not committed) and adjust if needed -- docker-compose reads this.
POSTGRES_DB=incidentcommand
POSTGRES_USER=incidentcommand
POSTGRES_PASSWORD=incidentcommand

MINIO_ROOT_USER=incidentcommand
MINIO_ROOT_PASSWORD=incidentcommand123
